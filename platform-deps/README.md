# 平台依赖

私有 jar 与安装脚本。脚本相对本目录查找 `lib/`，在本目录下执行即可。

```bash
# Linux / macOS / Git Bash
./install-lib.sh
./install-lib.sh /path/to/maven/repo

# Windows CMD
install-lib.bat
install-lib.bat C:\path\to\repository

# Windows PowerShell
.\install-lib.ps1
.\install-lib.ps1 -Repo C:\path\to\repository
```
